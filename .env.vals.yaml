# vals configuration for Google Cloud Secret Manager (YAML format)
# Converted from .teller.yml
# Usage:
#   eval $(vals env -f .env.vals.yaml)           # Export to current shell
#   vals exec -f .env.vals.yaml -- npm run build  # Run command with secrets

DOT_AI_AUTH_TOKEN: ref+gcpsecrets://vfarcic/dot-ai-auth-token
GITHUB_TOKEN: ref+gcpsecrets://vfarcic/github-token
